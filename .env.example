# Copy to .env.local and fill with values from Supabase → Project Settings → API.
# Use the Data API URL and the publishable (browser) key.

# Project URL only (https://xxx.supabase.co) — do not append /rest/v1
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Local seed scripts only (never commit, never use VITE_ prefix):
# SUPABASE_SERVICE_ROLE_KEY=
